import React  from 'react';
import { View,Text,StyleSheet} from 'react-native';

const Contacts =({ navigation }) => {
    return(
        <View>
            <Text>This is Contacts Tab</Text>
        </View>
    )
};

const styles = StyleSheet.create({}); 

export default Contacts;